ext {

    /* ======================== Google Official Extension ======================== */

    kotlin_core_version = "1.10.0"
    appcompat_version = "1.6.1"
    constraintlayout_version = "2.1.4"
    junit_version = "4.13.2"
    junit_ext_version = "1.1.5"
    espresso_ui_test_version = "3.5.1"

    // Compose Ui
    compose_bom_version = "2023.01.00"
    compose_compiler_version = "1.4.2"
    compose_activity_version = "1.7.1"
    compose_viewmodel_version = "2.6.1"

    // Accompanist Components
    accompanist_version = "0.31.2-alpha"

    // Navigation
    navigation_version = "2.5.3"
    navigation_hilt_version = "1.0.0"

    // Lifecycle
    lifecycle_ktx_version = "2.6.1"
    lifecycle_ext_version = "2.2.0"
    lifecycle_compose_version = "2.6.0"

    // Datastore
    datastore_version = "1.0.0"

    // Room
    room_version = "2.5.1"

    // Dagger Hilt
    dagger_hilt_version = "2.45"

    // Coroutine
    kotlinx_coroutines_version = "1.6.4"

    // WorkerManager
    work_version = "2.8.1"
    work_hilt_version = "1.0.0"

    // Paging
    paging_version = "3.1.1"
    paging_compose_version = "1.0.0-alpha19"

    // Location
    gms_location_version = "21.0.1"

    // Admob
    admob_version = "22.1.0"

    /* ======================== Third-party Extension ======================== */

    // Retrofit
    retrofit_version = "2.9.0"
    retrofit_gson_conveter_version = "2.9.0"
    retrofit_moshi_conveter_version = "2.9.0"

    // Moshi
    moshi_version = "1.14.0"

    // Coil
    coil_version = "2.2.2"

    // Lottie
    lottie_version = "3.4.0"
    lottie_compose_version = "6.0.0"

}